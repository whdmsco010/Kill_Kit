using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClikCheck : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("클릭성공");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("클릭성공");
    }
}
